spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql:
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    root: warn
    com.yalexin: info
  file: log/blog-pro.log

server:
  port: 8081
